import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aryamaan.sanctuary',
  appName: 'Aryamaan Sanctuary',
  webDir: 'dist',
  backgroundColor: '#FAF8F5'
};

export default config;
